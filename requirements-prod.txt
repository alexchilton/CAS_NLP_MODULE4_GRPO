# Production dependencies for CUDA-enabled training
# Use this for training on GPU-equipped machines (laptop with CUDA)
# Install with: pip install -r requirements-prod.txt

# Include all core dependencies
-r requirements.txt

# Quantization and memory optimization (CUDA-only)
bitsandbytes

# Experiment tracking and monitoring
wandb
tensorboard

# Note: Ensure you have CUDA-compatible PyTorch installed
# For CUDA 11.8: pip install torch --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1: pip install torch --index-url https://download.pytorch.org/whl/cu121
